# Pinned runtime dependencies for the packaged security sidecar.
#
# Installed into an isolated build venv by build_sidecar.py, so PyInstaller
# only bundles what the sidecar actually uses. Building from a global Python
# environment pulled PyTorch, OpenCV, pyarrow and HuggingFace Transformers
# into the bundle (~530 MB of unused code) via scikit-learn's optional
# array-API imports.
#
# scikit-learn, numpy and scipy MUST match the versions the models were
# trained with. The .joblib files are pickles of scikit-learn objects holding
# numpy arrays; a version mismatch can fail to unpickle or silently change
# behaviour. Retrain the models before changing these pins.

scikit-learn==1.5.2
numpy==2.3.2
scipy==1.16.3
pandas==3.0.5
joblib==1.5.2
threadpoolctl==3.6.0
flask==3.1.0
requests==2.32.4
pyusb==1.3.1
libusb-package==1.0.26.3
pyinstaller==6.18.0
